# 宝可梦橱窗

在线预览：
- 此项目通过使用`CSS`的内部嵌套功能，使得代码可读性增强。
- 利用了`:root`伪类来定义全局的CSS属性，这些变量可以在整个样式表中重复使用，更加方便修改。
- 精灵球使用了`background-image:radial-gradient()`和`background-image:linear-gradient()`，径向渐变来实现精灵球的圆环；线性渐变来做球的两半颜色和中间的黑色线段。
- 四个精灵使用id区分，分开做样式。